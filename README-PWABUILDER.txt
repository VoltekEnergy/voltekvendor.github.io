Voltek Energy Vendor — PWA package (relative paths; works at root or subpath).
1) Host these files on any HTTPS host (Netlify drag-drop, Vercel, Firebase, or GitHub Pages — keep .nojekyll).
2) Go to pwabuilder.com, enter your URL (include trailing slash for GitHub project sites).
3) Package for Stores → Android.
Connected to Supabase project fokiodbtpwlsntxdfaty — syncs with the Android app.
